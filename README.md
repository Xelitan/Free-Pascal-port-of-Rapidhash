# Usage
```
  Memo1.Lines.Add(IntToHex(RapidHash('hello world'), 16));
  Memo1.Lines.Add(IntToHex(RapidHashMicro('hello world'), 16));
  Memo1.Lines.Add(IntToHex(RapidHashNano('hello world'), 16));
```
